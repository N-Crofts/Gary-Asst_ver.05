# Copy to .env and fill in values.
# See README for full documentation.

# ---- Integrations ----
OPENAI_API_KEY=sk-xxxx
BING_API_KEY=xxxx
AZURE_CLIENT_ID=xxxx
AZURE_TENANT_ID=xxxx
AZURE_CLIENT_SECRET=xxxx
MAILBOX_ADDRESS=sorum.crofts@rpck.com

# ---- Research (Tavily) ----
# APP_ENV: development | production. Missing => development.
# RESEARCH_ENABLED: set true to enable research feature.
# ENABLE_RESEARCH_DEV: set true only to allow Tavily in development (override).
# TAVILY_ALLOW_ADVANCED: default false. Set true only to allow advanced Tavily ops (extract/map/crawl).
# Research runs only when allow_research=True at call site (digest preview, run-digest, digest send).
# Budget cap: 1 Tavily call per request (no retries, no parallel double-spend).
APP_ENV=development
RESEARCH_ENABLED=false
ENABLE_RESEARCH_DEV=false
TAVILY_ALLOW_ADVANCED=false

# ---- Optional ----
# TAVILY_API_KEY=xxxx
# DATABASE_URL=postgresql://user:pass@localhost:5432/gary
